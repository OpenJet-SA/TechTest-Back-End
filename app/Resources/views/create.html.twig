{% extends 'base.html.twig' %}

{% block body %}
  <form method="POST" action="{{ path('app_flight_create') }}">
    {{ form_widget(form)}}
    <button type="submit">
        Add
    </button>
  </form>
{% endblock body %}